#include <stdio.h>

int main() {
    int number;
    printf("Введіть ціле число: ");
    scanf("%d", &number);

    if (number % 2 == 0)
        printf("Число парне\n");
    else
        printf("Число непарне\n");

    return 0;
}
